CONFIG_VENDOR_LENOVO=y
CONFIG_BOARD_LENOVO_T480=y
CONFIG_IFD_BIN_PATH="binaries/ifd.bin"
CONFIG_ME_BIN_PATH="binaries/me.bin"
CONFIG_GBE_BIN_PATH="binaries/gbe.bin"
CONFIG_HAVE_IFD_BIN=y
CONFIG_HAVE_ME_BIN=y
CONFIG_HAVE_GBE_BIN=y
CONFIG_PAYLOAD_EDK2=y
CONFIG_EDK2_BOOTSPLASH_FILE="/opt/coreboot/splash.bmp"
CONFIG_EDK2_FOLLOW_BGRT_SPEC=y
CONFIG_MAINBOARD_SMBIOS_PRODUCT_NAME="ThinkPad T480"
# thinkpad_acpi probes DMI product_version for the word "ThinkPad"; the
# default "1.0" makes it refuse to load (modprobe: "No such device").
CONFIG_MAINBOARD_VERSION="ThinkPad T480"
# vboot: the RO section verifies the signed RW slots A/B before jumping in
# (docs/vboot-notes.md). Layout comes from vboot.fmd (patch 0040); with
# FMDFILE set, CBFS_SIZE no longer shapes the layout.
CONFIG_VBOOT=y
CONFIG_FMDFILE="src/mainboard/lenovo/sklkbl_thinkpad/vboot.fmd"
# the SoC selects VBOOT_MUST_REQUEST_DISPLAY - without this, normal verified
# boots skip display init entirely (no splash, dark until the kernel)
CONFIG_VBOOT_ALWAYS_ENABLE_DISPLAY=y
# nothing else clears the recovery request with a non-vboot payload: the only
# other caller sits in the depthcharge path. Without this a single recovery
# event pins the machine to RO boots forever.
CONFIG_VBOOT_CLEAR_RECOVERY_IN_RAMSTAGE=y
# own signing keys from keys/ (never committed). Drop these four lines to fall
# back to the public vboot devkeys.
# Firmware version for rollback protection. futility writes it into both slot
# preambles; vboot combines it with the keyblock's key version into
# sd->fw_version and refuses anything below the TPM counter. RAISE BY ONE FOR
# EVERY RELEASE that locks something out - the counter cannot move without it.
# Once the counter follows (scripts/vbnv.py boot-ok), every older image stops
# booting, backups included. The recovery path in WP_RO is not version-checked.
CONFIG_VBOOT_KEYBLOCK_VERSION=5
# coreboot defaults this GBB flag on (security/vboot/Kconfig), and it makes
# vboot skip the rollback comparison - 2firmware.c:189 logs "Ignoring FW
# rollback due to GBB flag" and boots anyway. The roll-forward twenty lines
# below has no such condition, so the counter advances and enforces nothing.
# Measured: a version-2 slot booted with the counter at 3. The flag sits in the
# GBB inside WP_RO, so changing it needs the external programmer.
# CONFIG_GBB_FLAG_FORCE_DEV_BOOT_USB is left at its default: it only feeds
# vb2_fill_dev_boot_flags(), which nothing outside the kernel phase calls.
# CONFIG_GBB_FLAG_DISABLE_FW_ROLLBACK_CHECK is not set
CONFIG_VBOOT_ROOT_KEY="/opt/keys/root_key.vbpubk"
CONFIG_VBOOT_RECOVERY_KEY="/opt/keys/recovery_key.vbpubk"
CONFIG_VBOOT_FIRMWARE_PRIVKEY="/opt/keys/firmware_data_key.vbprivk"
CONFIG_VBOOT_KEYBLOCK="/opt/keys/firmware.keyblock"
# SPI write protection for WP_RO (docs/vboot-notes.md). A PCH protected range
# covers WP_RO exactly at 4 KB granularity; the chip's own BP bits do
# power-of-two blocks only and would round up to the top 8 MB, taking half of
# RW_SECTION_B. The range is sealed with FLOCKDN/DLOCK every boot and blocks
# every host write including SMM, independent of bios_lock. SMMSTORE, the MRC
# cache and both slots stay writable; any WP_RO change - keyset, GBB, verstage,
# layout - needs the external programmer.
# BOOTMEDIA_LOCK_IN_VERSTAGE stays off: it would lock before ramstage and its
# help text warns it blocks write-protect facilities there; everything running
# between verstage and the ramstage lock is verified code anyway.
CONFIG_BOOTMEDIA_LOCK_CONTROLLER=y
CONFIG_BOOTMEDIA_LOCK_WPRO_VBOOT_RO=y
# Second range over SI_DESC + SI_GBE (patch 0043), the regions the first one
# and BIOS_CONTROL both miss. Write protection only, so a full-chip backup
# still reads them. Protected ranges bind the master that sets them, so the ME
# and the GbE controller keep their own access. Closes issue #5; after this the
# descriptor and the MAC need the external programmer.
CONFIG_BOOTMEDIA_LOCK_DESCRIPTOR_GBE=y
# SMM BIOS write protection: flash writes only succeed from SMM, so flashrom
# from a running OS stops working while SMMSTORE (which goes through SMM) keeps
# working. The runtime option puts a "BIOS Lock" toggle into the setup menu
# (patch 0042) - turn it off, reboot, flash, turn it back on. The toggle lives
# in SMMSTORE, writable from the OS, so this is a tripwire against accidents
# and remote software, not a boundary against root. That boundary is the
# controller lock above.
CONFIG_BOOTMEDIA_SMM_BWP=y
CONFIG_BOOTMEDIA_SMM_BWP_RUNTIME_OPTION=y
# Flash descriptor: without this coreboot defaults to UNLOCK_FLASH_REGIONS and
# runs `ifdtool -u`, handing the host write access to every region - descriptor,
# ME and GbE, none of them covered by SMM BWP or the WP_RO range (measured: a
# GbE write succeeds on a fully locked machine). Locking leaves the host
# read-only on the descriptor and out of ME writes; ALLOW_CPU_READ_ACCESS keeps
# ME readable for a full-chip backup. The BIOS region stays read/write, so slot
# updates are unaffected. ifdtool keeps GbE host-writable on this platform -
# that is what the range above closes. One-way: the descriptor needs the
# programmer afterwards.
CONFIG_LOCK_MANAGEMENT_ENGINE=y
CONFIG_ME_REGION_ALLOW_CPU_READ_ACCESS=y
CONFIG_SMMSTORE=y
CONFIG_SMMSTORE_V2=y
CONFIG_SMMSTORE_SIZE=0x40000
CONFIG_EDK2_SECURE_BOOT_SUPPORT=y
# --- ESRT, measurement (branch capsule-slot-only) ---------------------------
# Publishes an EFI System Resource Table so fwupd sees the firmware at all.
# This is the whole mechanism: BlSupportDxe sits unconditionally in DXEFV,
# reads coreboot's firmware-info HOB and installs the ESRT itself
# (UefiPayloadPkg/BlSupportDxe/BlSupportDxe.c). EsrtDxe and FmpDxe are a
# separate, FMP-based path and are not needed for this.
#
# Nothing here can apply a capsule. FmpDxe is not in the firmware at all - the
# dsc builds it only to be embedded into a capsule - and
# DRIVERS_EFI_CAPSULE_ACCEPT_EMBEDDED_DRIVERS is off, so a capsule carrying one
# is refused. The ESRT is a statement about the running firmware, nothing more.
#
# GUID is reproducible, not random:
#   uuid5(NAMESPACE_DNS, "system-firmware.custom-coreboot-t480.Amphero")
# Version follows coreboot's own encoding, (major << 16) | minor for 26.08.
# LSV 0 means "same as the current version".
CONFIG_DRIVERS_EFI_FW_INFO=y
CONFIG_DRIVERS_EFI_MAIN_FW_GUID="271c57fb-af28-5d13-96d4-c8ad68ef4ea1"
CONFIG_DRIVERS_EFI_MAIN_FW_VERSION=0x001A0013
CONFIG_DRIVERS_EFI_MAIN_FW_LSV=0x00000000
# The SMI handler hands out full-flash access only in a boot where coreboot
# found a capsule, so without this the writer cannot write at all. SLOT_UPDATE
# then picks the arrangement (patch 0044): FmpDxe stays in the firmware and
# writes the inactive slot instead of riding in the capsule to rewrite the whole
# chip. Without it the edk2 Makefile passes -D CAPSULE_SUPPORT=TRUE.
CONFIG_DRIVERS_EFI_UPDATE_CAPSULES=y
CONFIG_DRIVERS_EFI_CAPSULE_SLOT_UPDATE=y
# Capsule trust anchor: FmpDxe accepts only capsules whose PKCS#7 chain ends
# in this root certificate (patch 0045 makes the option visible without
# in-tree capsule generation; the edk2 Makefile converts it to the FmpDxe
# PCD). Generate the set with scripts/gen-capsule-certs.sh - keys/capsule/ is
# untracked like the rest of keys/. Dropping this line falls back to EDK2's
# published test certificates, which anyone can sign with.
CONFIG_DRIVERS_EFI_CAPSULE_TRUSTED_PUBLIC_CERT="/opt/keys/capsule/root.pub.pem"
# To see the payload's own DEBUG() output, add -D USE_CBMEM_FOR_CONSOLE=TRUE
# here. It routes SerialPortLib to CbSerialPortLib so the text lands in
# coreboot's CBMEM console (/sys/firmware/log), and it suppresses
# -DMDEPKG_NDEBUG in the dsc, which is what keeps DEBUG() alive in a RELEASE
# build. No EDK2_DEBUG and no oversized debug payload needed.
# EDK2 builds quiet (-q in payloads/external/edk2/Makefile), which swallows
# compiler and linker errors - a failing module then reports only "Failed to
# execute command". Append -v here to get them back; this list is added after
# -q, so it wins.
CONFIG_EDK2_CUSTOM_BUILD_PARAMS="-D VARIABLE_SUPPORT=SMMSTORE"
# measure all stages/blobs into TPM PCR 2 before running them (log: cbmem -L)
CONFIG_TPM_MEASURED_BOOT=y
# The payload re-logs coreboot's measurements into Tcg2Dxe's event log
# (patch 0005), so the one log the kernel reads carries the whole chain and
# the PCRs reconstruct. Linux prefers the ACPI-referenced log over the EFI
# one, so the ACPI reference has to go (patch 0047); cbmem -L still works.
CONFIG_TPM2_ACPI_NO_LOG_AREA=y
# (machine identity + optional devicetree devices live in config/board.conf)
